# Local dev (optional — defaults to http://localhost:4000/api)
# VITE_API_URL=http://localhost:4000/api

# Vercel (all-in-one): leave unset — production uses /api on same domain
# VITE_API_URL=/api

# Separate backend host (Render, etc.):
# VITE_API_URL=https://your-backend.onrender.com/api
