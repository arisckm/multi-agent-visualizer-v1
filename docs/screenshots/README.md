# Screenshots

Add PNG or JPG files here for the main README:

| File | Suggested content |
|------|-------------------|
| `canvas.png` | Full workspace: palette + graph + properties |
| `timeline.png` | Bottom run timeline after simulation |

Capture from local dev (`npm run dev`) or your Vercel deployment, then uncomment the image lines in the root `README.md`.
